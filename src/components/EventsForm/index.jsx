import { Formik, Form, Field, ErrorMessage } from 'formik';

function EventsForm () {
  return <section>
    <span>Форма заповлення для івентів</span>
    <Formik>
      
    </Formik>
  </section>;
}

export default EventsForm;
